<div>
	<table class="table table-hover table-striped">
		<tr>
			<th>Name</th>
			<th>Users</th>
			<th></th>
			<th></th>
		</tr>
		<% @roles.each do |role| %>
			<tr>
				<td><%= role.name %></td>
				<td>...</td>
				<td><%= link_to (raw '<i class="icon-pencil"></i>'), edit_work_place_role_path(@work_place, role) %></td>
				<td><%= link_to (raw '<i class="icon-trash"></i>'), [@work_place, role], method: :delete, data: { confirm: 'Are you sure?' } %></td>
			</tr>
		<% end %>
	</table>
</div>
<div>
	<%= link_to 'Create new', new_work_place_role_path(@work_place), class: 'btn btn-success' %>
</div>