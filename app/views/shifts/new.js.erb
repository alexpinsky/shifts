var new_shift_modal = $("#<%= @container_id %>");
new_shift_modal.find(".modal-body").html("<%= j(render partial: 'shift_form', locals: { workplace: @workplace, shift: @shift }) %>");
initTimePickers();
new_shift_modal.modal();
